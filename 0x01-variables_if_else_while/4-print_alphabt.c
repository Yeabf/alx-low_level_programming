#include <stdio.h>

/**
 *main- print letters except q and e
 *Return: is 0
 */

int main(void)
{
	char alpha = 'a';
	
	while (alpha <= 'z', !'q', !'e');
	{
		putchar(alpha);
		alpha++;
	}
	return (0);
}

